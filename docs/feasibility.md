# Netflix 双轨字幕可行性

状态：待真实 Netflix 验证，未通过阶段 0。

当前只有 WXT 工程骨架和只读播放器检测。内容脚本按需检查播放页、video 元素与标准 TextTrack 数量，不提取字幕内容，不修改字幕模式。TextTrack 数量不能代表 Netflix 完整字幕菜单；弹窗不会将它作为可选语言列表。

## 待验证

- 当前影片网页字幕菜单的完整轨道列表与稳定标识。
- 两种不同语言字幕能否同时获取，不依赖持续切换原生字幕。
- 字幕格式、请求来源、最小权限和可能的页面上下文桥接。
- 两种语言独立按视频时间同步，处理 seek、全屏与切集。
- 两部影片、Chrome / Edge，以及含三种语言影片的组合切换。

尚无可记录的真实 Netflix 浏览器版本、轨道格式或字幕 CDN 域名。不要据此文件宣称自动双语显示已支持。

若无法实现，按 PLAN.md 报告阻碍并重新讨论范围，不自动改成字幕导入或机器翻译。
## 实验性字幕列表读取（2026-09-20）

已增加 MAIN world 页面桥接，尝试调用 Netflix 私有播放器的 getTimedTextTrackList / getTimedTextTrack。此接口不是官方承诺，当前结构仍需真实页面验证；它与 video.textTracks 不同。

只返回裁剪后的轨道 ID、语言、标签和当前轨道标识，不传输播放器对象或字幕下载地址。桥接验证来源、请求 ID 和数据结构，并设置超时。多个可用播放器时拒绝猜测。页面数据仅用于展示，没有特权请求入口。

用户已确认旧版页面脚本连接成功并找到视频，但标准轨道为 0。新版需要核对是否读到日语及完整菜单；仍未证明双轨内容可获取。

用户实测反馈：2026-09-20，新版读取到 37 条轨道，当前字幕正确识别为 Japanese。已验证列表读取入口和当前字幕识别；网页菜单完整一致性、双轨内容及同步尚未验证。
