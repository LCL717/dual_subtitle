# Netflix 双语字幕工具：项目计划

- 项目暂定名：Dul Subtitle
- 日期：2026-09-20
- 状态：已确认使用 WXT，工程骨架已创建并通过类型检查与双浏览器构建；Netflix 双轨获取待实测
- 项目位置：当前 dul_subtitle 目录

## 1. 已确认的目标与默认假设

在桌面版 Chrome 和 Microsoft Edge 中观看 Netflix 时，用户可以从当前影片在 Netflix 网页字幕菜单中提供的语言里任意选择两种不同语言，由扩展在播放器内同时显示。扩展提供独立的“上方字幕”和“下方字幕”选择框，用户决定语言组合与上下顺序，也可以调整字号、位置并随时关闭。

这里将“双行字幕”理解为“双语字幕”：每种语言占一个字幕区域，长句允许自动换行。语言组合不固定为中英，也不提供脱离当前影片字幕菜单的全球语言清单。首次使用由用户选择两种语言，之后优先恢复已保存且当前影片仍支持的偏好。

首版面向个人本地使用，通过浏览器开发者模式加载。需要用户已有 Netflix 账号和正常播放权限。不要求服务器、付费翻译 API 或额外账号。

## 2. 产品形态

采用 Manifest V3 浏览器扩展，在 Netflix 播放页叠加字幕，并提供扩展弹窗和播放器内的简洁设置入口。

独立网页不能直接访问另一个 Netflix 标签页的播放器和字幕状态；浏览器扩展更适合这个使用场景。Chrome 的 content scripts 支持读取和修改目标页面 DOM，Edge 支持迁移 Chromium 扩展，但两款浏览器仍分别验收。[1][2]

首版支持 Windows 桌面浏览器、普通播放和网页全屏。移动端、Netflix 桌面应用、投屏和画中画暂不包含在验收范围内。

## 3. 首版范围

| 功能 | 首版行为 |
| --- | --- |
| 双语显示 | 同时显示用户选定的两种不同语言，支持任意可选组合与上下顺序 |
| 语言选择 | 两个独立选择框，均以当前影片 Netflix 网页字幕菜单为准；保留普通字幕、SDH 等标签，不混入音轨或“关闭”选项 |
| 播放同步 | 跟随播放、暂停、拖动、变速和跳过片头更新字幕 |
| 样式 | 两种语言独立字号、文字颜色、背景透明度、底部间距 |
| 开关 | 一键启停，关闭或出错后恢复 Netflix 原生字幕 |
| 设置保存 | 本机保存语言偏好和样式；切换影片后重新匹配可用轨道 |
| 状态提示 | 区分加载中、未开启字幕、缺少目标语言、读取失败 |
| 页面生命周期 | 处理页面内跳转、连续播放下一集、播放器重建和全屏切换 |

暂不做：AI 翻译、语音识别、查词与生词本、字幕下载、云同步、其他视频网站和扩展商店发布。

## 4. 首要风险：双轨字幕获取

浏览器扩展能力已经有官方文档依据；Netflix 当前页面是否能稳定提供两条字幕轨道，尚未实测，不能把它当作已解决的问题。

Netflix 提供的语言会因影片等条件不同而变化，不能承诺某个固定语言组合始终存在。[3] 原生界面一次只能选择一种字幕，本项目的核心正是由扩展独立选择并同时显示两种语言。原生字幕 DOM 通常只能反映正在显示的字幕，读取当前字幕不等于获得第二条语言轨道。

### 阶段 0：先完成可行性验证

在用户已登录的真实 Netflix 播放页验证以下项目，不收集账号密码或导出会话信息：

1. 定位当前视频、播放时间、影片标识与播放器容器。
2. 读取当前影片字幕选项并与 Netflix 网页字幕菜单对照，确保语言、标签与轨道对应正确。检查标准 TextTrack 是否可用；若不足，再评估页面已加载的字幕元数据、轨道信息和字幕请求。
3. 验证能否获取两个不同语言的完整或分段字幕内容，以及实际格式，例如 WebVTT 或 TTML。格式以实测为准。
4. 验证第二轨加载不会持续切换原生字幕、影响音轨或打断播放。
5. 检查字幕请求的来源、权限、跨域和会话约束；确定是否确实需要页面上下文桥接或扩展后台请求。
6. 用至少两部提供多种字幕语言的影片，在 Chrome 和 Edge 中验证普通播放、拖动和全屏；至少一部提供三种或以上语言，用于验证不同组合、非中英组合和上下语言交换。

阶段输出：`docs/feasibility.md`，记录可复现步骤、浏览器版本、可用格式、所需最小权限、失败场景和通过/不通过结论。报告不包含 Cookie、带凭据的请求地址或整份影片字幕。

**通过条件：** 语言列表与当前影片网页字幕菜单一致，用户所选的两条不同语言字幕能同时获取并独立同步，不依赖用户反复操作原生单选菜单；切换组合、交换上下顺序、拖动和切集后无旧字幕残留，不影响原生播放。通过后才进入自动双轨功能开发。

**不通过时：** 明确说明阻碍，不把单轨显示视为完成。可提供“Netflix 原生第一语言 + 用户导入本地 SRT / VTT 第二语言”的替代方案；这会增加手动操作，需要重新确认产品范围。实时翻译仅作为后续独立选项讨论，不默认接入。

## 5. 技术设计

建议技术栈：TypeScript + WXT（底层使用 Vite）+ Manifest V3。小型弹窗与设置界面使用原生 DOM / CSS；首版无需引入大型 UI 框架。测试使用 Vitest，浏览器模拟页面测试可使用 Playwright。

模块职责：

- `content`：识别播放页、监听播放器变化、管理生命周期。
- `netflix-adapter`：封装 Netflix 特有的轨道发现、字幕获取、影片切换与原生字幕状态恢复。将网页字幕菜单选项映射到稳定的轨道标识、语言标识和显示标签，不硬编码中英轨道。
- `page-bridge`：仅在隔离环境无法满足需要时使用；页面与扩展之间只传递必要字幕及播放数据。
- `subtitles`：将实际验证过的格式转换为统一 cue 数据：开始时间、结束时间、文本、语言。
- `sync`：以视频当前时间分别查找两种语言的有效 cue，不按两轨数组下标配对。
- `overlay`：在播放器容器中渲染字幕，尽量使用 Shadow DOM 隔离样式。
- `popup` / `settings`：提供上方、下方两个字幕选择框以及显示选项、开关和状态；区分语言与同语言的不同字幕轨道。
- `storage`：通过 chrome.storage.local 保存偏好；字幕内容默认只保存在内存中。
- `background`：按验证结果决定是否加入 service worker；不依赖其持续运行进行字幕计时。

双语轨道断句、时长和 cue 数量可能不同，每条轨道独立按时间查询；处理重叠 cue、空白区间与多行内容。渲染只在显示内容发生变化时更新。视频播放时使用合适的帧回调或轻量计时器，暂停时停止不必要的轮询。

切换影片时取消旧请求、清空旧字幕并重建同步状态，避免异步响应串片。监听 seek、ratechange、fullscreenchange 等事件；全屏时确保叠加层位于实际全屏元素内部。

仅在自定义字幕已可用时隐藏原生字幕，并保存原状态。扩展关闭、故障或卸载清理时恢复，避免三份字幕重叠或出现没有字幕可看的情况。

## 6. 权限与数据处理

- 内容脚本仅匹配所需 Netflix 页面；支持从浏览页进入播放页的单页导航。
- 默认仅申请 storage 和必要的 Netflix 页面访问范围。
- 如果字幕来自额外域名，按实测结果明确列出最小 host permissions；不申请所有网站访问权限。
- 不预设需要 debugger、cookies 或广泛网络监听权限。
- 页面桥接消息限定来源、结构、类型和大小；来自页面的数据不能任意触发扩展特权请求。
- 字幕以文本方式渲染，不将字幕内容直接作为 HTML 插入。
- 不上传观看记录和字幕；无遥测、无远程执行代码；不接触受保护的视频流或 DRM 解密。

## 7. 用户流程与界面草案

1. 用户加载扩展，进入 Netflix 播放页。
2. 扩展读取当前影片在 Netflix 网页上可选的字幕语言，加载期间显示状态。
3. 用户在扩展的“上方字幕”和“下方字幕”中选择两种不同语言并开启双语显示，无需在 Netflix 原生菜单中反复切换。首次使用不固定中英组合；已有偏好在当前影片可用时恢复，缺失时提示重新选择，不自动换成其他语言。
4. 播放器底部显示两组字幕，设置入口用于调整字号、位置及开关。
5. 切集后重新读取字幕列表、匹配偏好并加载；关闭扩展字幕时恢复原生字幕。若不足两种语言或任一所选轨道读取失败，说明原因，保留或恢复原生字幕，不静默缩减为单语成功状态。

```text
┌───────────────────────────────────────┐
│             Netflix 视频               │
│                                       │
│             そろそろ行こう。           │
│           我想我们现在该走了。          │
│                                       │
│                       [双语字幕 · 设置]│
└───────────────────────────────────────┘
```

上图仅以“上方日语、下方中文”为例。设置区提供两个独立选择框，例如“上方字幕：[日语 ▼]”“下方字幕：[简体中文 ▼]”；两个列表均来自当前影片的 Netflix 网页字幕选项。用户也可以改为英语 + 韩语等当前可选组合。

设置需要可键盘操作，字幕不遮挡播放控制条；短窗口和长句情况下允许自动换行。手动同步偏移仅在真实测试表明需要时加入。

## 8. WXT 目录结构（已创建骨架）

```text
dul_subtitle/
  PLAN.md
  README.md
  package.json
  package-lock.json
  tsconfig.json
  wxt.config.ts              # 扩展配置，WXT 自动生成 manifest.json
  entrypoints/
    netflix.content.ts      # Netflix 页面只读检测
    popup/
      index.html
      main.ts
      style.css
  lib/
    protocol.ts             # 页面与弹窗的消息约定
  docs/
    feasibility.md
    manual-test.md
  .wxt/                     # 生成的类型与配置，不提交
  .output/                  # Chrome / Edge 构建，不提交
```

字幕适配、解析、同步、叠加层及测试目录在阶段 0 验证后按需增加。后台模块和页面桥接仅在验证需要时实现。测试字幕使用自建小样本。

## 9. 实施顺序与交付物

| 阶段 | 工作 | 完成标准 |
| --- | --- | --- |
| 0：可行性 | 实测双轨获取、播放时间与全屏 | feasibility.md 给出明确结论 |
| 1：项目骨架 | 构建、manifest、内容脚本、弹窗 | 同一构建可在 Chrome / Edge 加载 |
| 2：字幕核心 | 已验证格式解析、双轨同步、叠加层 | 普通播放、暂停与拖动正确显示 |
| 3：使用体验 | 设置保存、切集、全屏、状态和恢复 | 完成端到端观看流程 |
| 4：验证交付 | 自动检查、真实浏览器验收、说明文档 | 可复现构建、安装和使用步骤 |

已根据用户指示先创建 WXT 骨架：弹窗、语言选择占位、字号预览与保存、Netflix 页面检测。类型检查与两款浏览器构建通过；真实浏览器加载仍待手动验证。阶段 0 与阶段 1 的基础工程工作可并行，字幕核心开发仍以阶段 0 通过为前提。工期在阶段 0 后评估。

## 10. 测试与验收

自动化检查关注字幕边界与时序：格式解析、重叠 cue、两轨断句不同、空白区间、seek 后清理、旧影片异步响应丢弃。使用模拟播放器检查叠加层生命周期与故障恢复；这不能替代真实 Netflix 验证。

真实验收记录 Chrome 和 Edge 的具体版本，在各自浏览器覆盖：

- 同一构建可以加载并正常启停，无未处理异常。
- 扩展语言列表与当前影片 Netflix 网页字幕菜单一致，不混入音轨、其他影片语言或翻译生成的语言。
- 在至少三种字幕语言的测试影片中覆盖所有不同语言的两两组合，并验证上下顺序交换；包含非中英组合。两种字幕同时显示，无原生字幕重复覆盖。
- 更换任一选择后更新对应字幕且无旧字幕残留，无需反复切换原生菜单；菜单中存在但读取失败的轨道必须明确报错，不静默隐藏以缩小支持范围。
- 首次使用可自由选择；切集后重读列表并恢复可用偏好，缺少偏好语言或不足两种语言时提示处理。
- 播放、暂停、前后拖动、跳片头、变速和连续播放下一集后字幕正确。
- 以字幕 cue 时间和 video.currentTime 比较，正常播放时目标显示误差不超过约 200ms；缓冲期间不沿用独立墙上时钟推进字幕。
- 普通窗口、全屏和窗口缩放后字幕可见，控制条可操作。
- 缺少语言、字幕为空、请求失败和暂未识别播放器时有清晰状态，原生播放可继续使用。
- 关闭功能后恢复原生字幕，刷新页面保留设置，离开播放页清理监听器及定时器。

交付：可加载的扩展构建、源代码、README 安装步骤、已通过的检查记录以及已知限制。未经真实 Netflix 测试的项目必须标注“待验证”，不宣称已支持。

## 11. 最终效果与 review 范围

**已经确认的最终效果：** Netflix 原生一次只能显示一种字幕；安装本扩展后，用户可以从同一影片网页提供的字幕语言中任意选择两种不同语言，分别放在上方和下方，同时随视频同步显示。语言选项由 Netflix 当前影片决定，双语选择与同时显示由扩展负责。

例如影片提供英语、日语、韩语、简体中文，用户可以选择“日语 + 简体中文”或“英语 + 韩语”，也可以交换上下顺序。无需导入字幕或使用机器翻译，即可完成核心流程。

已确认需求与其余计划假设：

1. 产品形态：Chrome / Edge 扩展。
2. 已确认：从当前影片 Netflix 网页字幕语言中任意选择两种不同语言；扩展提供上下两个独立选择框并同时显示，长句可换行。
3. 首版定位：个人本地使用，无服务器、无 AI 翻译。
4. 优先顺序：先证明自动双轨可行，再完善样式和交互。
5. 获取失败：先报告技术阻碍；单语、手动切换、导入字幕或机器翻译均不视为已实现上述核心目标。本地导入等替代方案仅在后续重新确认范围后考虑。

## 12. 参考资料

以下资料于 2026-09-20 检索，仅支撑浏览器能力和 Netflix 字幕语言说明，不代表 Netflix 双轨方案已验证。

1. [Chrome：Content scripts](https://developer.chrome.com/docs/extensions/develop/concepts/content-scripts)
2. [Microsoft：Port a Chrome extension to Microsoft Edge](https://learn.microsoft.com/en-us/microsoft-edge/extensions/developer-guide/port-chrome-extension)
3. [Netflix：How to use subtitles, captions, or choose audio language](https://help.netflix.com/en/node/372)
4. [Chrome：Manifest file format](https://developer.chrome.com/docs/extensions/reference/manifest)

## 开发进展：字幕列表探测

已增加实验性 Netflix 私有播放器只读适配和 MAIN world 桥接。语言列表读取成功后可进行上下选择预览；不保存语言偏好，不改变原生字幕。六项模拟测试和两款浏览器构建通过，真实语言菜单一致性及双轨内容获取仍待验证。

