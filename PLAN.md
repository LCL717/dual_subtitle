# Netflix 双语字幕工具：进度与开发计划

- 项目：Dul Subtitle
- 更新日期：2026-09-20
- 当前阶段：双语字幕核心流程已实测成功，进入使用体验完善与兼容性验收阶段。
- 技术栈：TypeScript + WXT（底层 Vite）+ Manifest V3 + 原生 HTML/CSS。
- 本文区分目标、已实现能力和验证范围；代码存在不等于所有真实场景均已验收。

## 1. 已确认的最终目标

在 Windows 桌面 Chrome / Edge 的 Netflix 播放页面，用户从当前影片网页提供的字幕语言中任意选择两种不同语言，通过扩展的“上方字幕”和“下方字幕”选择框同时显示。

语言组合不固定为中英；两种语言各占一个区域，长句允许换行。语言选项来自当前影片的字幕轨道，不混入音轨，也不提供影片没有的翻译语言。普通字幕、SDH 和其他变体需保持可区分。

最终日常使用流程：首次选择两种语言 → 同时显示并跟随播放 → 保存偏好 → 下一集自动匹配并加载 → 缺少语言时提示选择 → 关闭后恢复原生字幕。

首版面向个人本地使用，不需要服务器、额外账号或付费翻译 API。需要已有 Netflix 正常播放权限。移动端、桌面应用、投屏和画中画不在首版范围内。

## 2. 当前完成情况

| 功能 | 当前实现 | 验证状态 |
| --- | --- | --- |
| WXT 工程 | 类型检查、Chrome / Edge 构建、本地加载说明、Git 与忽略规则 | 两套构建通过；用户已加载并使用 |
| 播放器连接 | 扩展弹窗与 Netflix 页面双向通信、视频检测 | 用户实测成功 |
| 字幕列表 | 读取 Netflix 内部播放器轨道，识别当前字幕 | 用户读到 37 条轨道，正确识别 Japanese；与网页菜单逐项一致性尚待核对 |
| 双语言选择 | 两个选择框、不同语言校验、SDH 标签、未知同名变体编号 | 用户选择 Japanese [SDH] + English [SDH] 成功 |
| 资源发现 | 从播放器内部状态按轨道 ID 查找资源，限制扫描范围与时间 | 用户实测两条轨道匹配 2/2，格式均为 imsc1.1 |
| 下载与解析 | 页面内请求、超时与大小限制、IMSC/TTML 基本文本和时间解析 | 当前影片双轨流程获用户成功反馈，解析单元测试通过 |
| 双语同步 | 两轨分别以 video.currentTime 查询，处理断句差异、重叠与空白区间 | 用户确认双语显示成功；时间边界和前后 seek 自动测试通过 |
| 叠加与恢复 | Shadow DOM 叠加层；两轨就绪后临时隐藏原生字幕，关闭时恢复 | DOM 模拟测试通过；真实场景专项验收仍需补齐 |
| 页面生命周期 | 取消加载、丢弃过期响应，影片或播放器切换时停止并恢复 | 已实现；切集后需要用户重新开启 |
| 字号 | 16–36 px 预览与本机保存，开启字幕时应用 | 已实现；调整后需重新开启，上下字号尚不独立 |
| 界面整理 | 主界面保留状态、语言、样式和开关；排查工具默认折叠 | 已完成，保留必要错误提示 |

**实测结论：** 用户在当前影片上确认日语与英语双语字幕完全成功。尚未记录该次测试的影片名称、浏览器具体版本及全部操作场景，不能推断所有影片、所有语言组合和两款浏览器均已通过。

**自动验证：** 最近一次 24 项测试、TypeScript 检查、Chrome / Edge 构建均通过。测试使用 Node 内置 test runner、XML 测试解析器和 Happy DOM；没有使用原计划中的 Vitest / Playwright。

## 3. 当前已知限制

- 不保存语言偏好；重新打开弹窗时不能恢复上下选择，尽管播放状态可以读取。
- 切集或播放器重建会停止双语字幕并恢复原生显示，尚不自动加载下一集。
- 字号调整需要重新开启；未提供独立字号、位置、文字颜色和背景透明度设置。
- 只支持已实现的 IMSC/TTML 文本子集：常见时钟、帧、tick 时间、父级时间偏移、文本 span 和换行。
- WebVTT、图像字幕、段内独立计时、顺序时间容器及复杂排版尚不支持；部分不支持的结构会明确报错。
- Netflix 私有接口可能变化；资源查找有上限，未找到不能视为影片没有字幕。
- 字幕请求仍可能遇到跨域、过期地址或网络失败；没有完整的刷新地址与自动重试策略。
- 暂停时仍保留轻量轮询用于位置和生命周期检查，性能优化待做。
- 全屏已具备处理逻辑；直接将 video 元素全屏时无法叠加，会恢复原生字幕。真实 Netflix 全屏兼容性待专项测试。

## 4. 后续路线与完成标准

### P1：记住语言选择，恢复当前状态

下一项优先开发。

- 在本机保存上下语言及字幕类型偏好，不把上一部影片的 trackId 当作跨影片标识。
- 当前标签页的实际播放状态与全局默认偏好分开管理，避免多个标签页互相覆盖正在使用的组合。
- 重新打开弹窗时恢复实际选择、启停状态和字号；明确区分正在显示的组合与尚未应用的选择。
- 当前影片缺少偏好语言或变体时提示选择，不静默换成别的语言。

完成标准：关闭再打开弹窗、刷新页面、换一部影片后，状态与偏好行为一致；同语言不同变体不混淆，多标签页互不干扰。

### P2：下一集自动继续双语字幕

依赖 P1 的语言偏好和状态管理。

- 检测影片标识、页面导航及播放器重建，取消旧请求并清空旧字幕。
- 等待新影片列表可用，按偏好匹配新轨道并加载，成功后恢复叠加。
- 在加载期间保留原生字幕；缺少语言、资源未就绪或请求失败时显示明确状态。
- 用户主动关闭后保持关闭，不因切集而自行重新开启。

完成标准：连续播放下一集无需重复选择；没有上一集残留字幕，快速切集不会串片；不足两种语言时可以继续原生播放。

### P3：实时样式调整

- 字号修改直接应用，不重新下载字幕。
- 上下两种语言独立字号，以及底部间距、颜色、背景透明度。
- 保存样式并在刷新后恢复，支持恢复默认值。
- 调整窄窗口、长句和全屏布局，保证播放控制条可操作。

完成标准：设置即时生效且不影响视频播放、字幕计时；普通窗口和全屏行为一致。

### P4：兼容性、稳定性与性能

可与 P1–P3 交叉推进，正式交付前完成关键场景验收。

- 在 Chrome 和 Edge 分别记录浏览器版本、测试影片和结果。
- 至少两部影片；至少一部提供三种以上字幕语言，覆盖两两组合、非中英组合及上下顺序交换。
- 逐项核对扩展列表与 Netflix 网页字幕菜单，区分 SDH、普通字幕及变体。
- 覆盖暂停、前后拖动、跳片头、倍速、缓冲、切集、全屏和窗口缩放。
- 验证两轨准备失败、加载中关闭、快速换组合、页面离开后清理及原生字幕恢复。
- 根据实际失败样本扩展字幕格式支持，不能用跳过异常轨道缩小支持范围却不告知用户。
- 优化资源扫描和内存缓存，缓存必须绑定影片；处理过期地址、有限重试与暂停时的无效轮询。

完成标准：记录可复现验收结果；以 cue 时间和 video.currentTime 比较，正常播放显示误差目标不超过约 200 ms，缓冲或暂停时不按独立墙上时钟推进字幕。尚未测量的场景保持“待验证”。

### P5：交付整理（功能稳定后）

- 完善图标、版本号、安装包和新手安装说明。
- 整理故障排查、已知限制、权限说明与测试记录。
- 根据需要决定是否加入播放器内设置入口。
- 扩展商店发布作为后续单独事项，当前不自动发布。

完成标准：从干净检出可以按锁文件安装、构建，并在两款浏览器复现安装及使用流程。

## 5. 当前技术结构

```text
dul_subtitle/
  PLAN.md
  README.md
  package.json
  package-lock.json
  tsconfig.json
  wxt.config.ts
  entrypoints/
    netflix.content.ts         # 消息协调、加载状态、叠加生命周期
    netflix-main.content.ts    # 页面上下文中的播放器读取与字幕获取
    popup/
      index.html
      main.ts
      style.css
  lib/
    protocol.ts                # 扩展消息与状态验证
    netflix-tracks.ts          # 字幕轨道与当前字幕
    subtitle-resources.ts      # 资源检查和摘要
    resource-discovery.ts      # 按轨道 ID 有界查找资源
    load-subtitles.ts          # 字幕下载、取消和大小限制
    imsc.ts                    # IMSC/TTML 文本解析
    timeline.ts                # 单轨时间查询，双轨独立使用
    dual.ts                    # 双语状态和数据校验
    overlay.ts                 # 显示、同步与原生字幕恢复
  tests/                       # 24 项现有自动测试
  docs/
    feasibility.md             # 技术验证过程与发现
    manual-test.md             # 测试记录及待验收项
  .wxt/                        # 自动生成，不提交
  .output/                     # Chrome / Edge 构建，不提交
  node_modules/                # 依赖，不提交
```

当前不需要后台 service worker。字幕地址与播放器对象不跨页面桥接暴露给弹窗；解析后的必要 cue 经校验后交给内容脚本显示，字幕内容仅保留在内存中。原生字幕通过临时样式隐藏，不通过持续切换原生语言实现双语效果。

## 6. 权限与范围约束

- 内容脚本仅匹配 `https://www.netflix.com/*`，当前扩展权限仅为 storage。
- 字幕请求在页面内发起，当前不增加额外 CDN host permissions。
- 不读取 Cookie、不上传字幕或观看记录、不接触视频 DRM 解密。
- 桥接限制来源、请求标识、字段结构和数据大小；字幕按文本渲染，不直接注入 HTML。
- 保留正常错误提示，详细排查默认折叠，不显示带凭据的字幕地址。
- 暂不做 AI 翻译、语音识别、查词、生词本、字幕导出下载、云同步和其他视频网站。为播放而获取字幕文件属于核心功能，不等同于提供下载导出工具。
- 单语、用户反复切换原生字幕、本地导入或机器翻译不能算作既定双语目标已完成；改变范围需另行讨论。

## 7. Git 里程碑

| 提交 | 内容 |
| --- | --- |
| 36a7234 | WXT 工程骨架与播放器诊断 |
| 2241e81 | 页面桥接读取 Netflix 字幕列表 |
| a7e6de2 | 双轨资源发现与时间轴基础 |
| d543598 | 用户实测成功的双语字幕播放版本 |
| 4127ffb | 精简调试界面，默认折叠排查工具 |

生成文件通过 .gitignore 排除；源码、测试、文档与 package-lock.json 纳入版本控制。本次计划更新以以上实现与用户反馈为基线，不将后续规划列为已完成。

## 8. 参考与验证记录

- [Chrome：Content scripts](https://developer.chrome.com/docs/extensions/develop/concepts/content-scripts)
- [Microsoft：Chrome 扩展迁移到 Edge](https://learn.microsoft.com/en-us/microsoft-edge/extensions/developer-guide/port-chrome-extension)
- [Netflix：字幕与音频语言说明](https://help.netflix.com/en/node/372)
- [WXT：安装与工程初始化](https://wxt.dev/guide/installation)
- [W3C：TTML2 时间基准与语法](https://www.w3.org/TR/ttml2/)
- 项目验证过程：`docs/feasibility.md`；检查与实测记录：`docs/manual-test.md`。

外部资料解释平台能力及格式；当前实现的可用性以代码测试和明确记录的用户实测为准，不宣称完整 IMSC 标准兼容。

## 优先修复：广告后自动同步

已实现播放器时间桥接，替换原来的 HTML 视频时间；广告标记出现或时间未就绪时恢复原生字幕，新正片时间到达后自动恢复双语。此次共 27 项测试及两款浏览器构建通过。

当前为待实测修复：Netflix 的广告 DOM 标记识别、播放器 getCurrentTime 在广告前后的语义仍需真实广告验证。请先完成广告前/中/后、多段广告、广告后暂停与拖动验收，再推进 P1。此前日英双语成功记录不代表广告场景已通过。

## 字幕样式设置（2026-09-20）

广告同步版本已提交为 c9ea665，真实广告验证仍待完成。其后新增样式：默认字幕背景完全透明，文字阴影开启；提供字号、背景不透明度（0–100%）、阴影开关和恢复默认。设置保存在本机，实时应用到已开启的字幕，不重新下载或重建时间轴。保留旧版字号偏好，其他样式使用新默认值。

29 项自动测试、类型检查及 Chrome / Edge 构建通过。P3 的共用字号实时生效、背景透明度与默认样式已实现；上下独立字号、颜色及位置设置仍待开发。样式修改影响当前浏览器中已开启本扩展的 Netflix 页面。
